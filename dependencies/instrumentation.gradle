ext {
    def JUnit = "1.1.0"
    def Espresso = "3.2.0"
    def TestRunner = "1.2.0"
    def MockWebServer = "3.12.0"

    androidTestImpl = [
            junit          : "androidx.test.ext:junit:${JUnit}",
            espressoCore   : "androidx.test.espresso:espresso-core:${Espresso}",
            espressoContrib: "androidx.test.espresso:espresso-contrib:${Espresso}",
            testRunner     : "androidx.test:runner:${TestRunner}",
            rules          : "androidx.test:rules:${TestRunner}",
            mockWebServer  : "com.squareup.okhttp3:mockwebserver:${MockWebServer}"
    ]
}